doctype html
html
    head
        meta(charset="utf-8")
        title Third Page
    body
        h1 Third Page

        script(src="../../bower_components/requirejs/require.js")
        script(src="../unit/config.js")
        script.
            require(['src/controler', 'test/mul-pages/usercase'], function (Controler, usercase) {
                new Controler().exec(usercase)
            })
